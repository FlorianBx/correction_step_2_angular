import { IsVegetarianDirective } from './is-vegetarian.directive';

describe('IsVegetarianDirective', () => {
  it('should create an instance', () => {
    const directive = new IsVegetarianDirective();
    expect(directive).toBeTruthy();
  });
});
